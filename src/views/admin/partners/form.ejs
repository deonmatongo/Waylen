<%- include('../../partials/scaffold-notice', {
      phase: 'Phase 3 — ecosystem',
      detail: 'The editor UI is scaffolded; store/update in partner.controller.ts persist it, including admin-only contract and commission terms.'
    }) %>

<section class="panel">
  <p class="panel__note">
    Route, controller action and navigation are in place. The form fields map
    directly onto the Prisma model — see
    <code>prisma/schema.prisma</code>.
  </p>
</section>
