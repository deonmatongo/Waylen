<%- include('../../partials/scaffold-notice', {
      phase: 'Phase 2 — operations',
      detail: 'The line-item builder is scaffolded; billingService.createInvoice already allocates gap-free invoice numbers inside the insert transaction.'
    }) %>

<section class="panel">
  <p class="panel__note">
    Route, controller action and navigation are in place. The form fields map
    directly onto the Prisma model — see
    <code>prisma/schema.prisma</code>.
  </p>
</section>
